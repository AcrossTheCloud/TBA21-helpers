version: 0.2

phases:
  install:
    commands:
      - npm install
  post_build:
    commands:
      - rm -rf test_files
      - npm install -g serverless@1.48.1
      - chmod -R +x bin
      - serverless deploy
